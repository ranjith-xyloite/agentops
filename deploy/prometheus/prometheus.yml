global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'agentops-backend'
    metrics_path: '/api/metrics'
    static_configs:
      - targets: ['backend:8000', 'host.docker.internal:8000']
